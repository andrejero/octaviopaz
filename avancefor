import java.util.Scanner;
public class Promedio1{
    public static void main(String[] args) {
        Scanner entrada = new Scanner(System.in);
        String nombre_alumno,materia,grupo, materia_alta, materia_baja;
        double cal,promedio, calificacion_alta, calificacion_baja;

            
              
        System.out.println("De cuantos alumnos deseas capturar informacion?");
        int numero_alumno=entrada.nextInt();
        entrada.nextLine();

        for(int i=1; i <numero_alumno; i++){

        System.out.println("Ingresa el nombre del alumno");
        nombre_alumno = entrada.nextLine();
        nombre_alumno=nombre_alumno.toUpperCase();
        System.out.println("Ingresa el grupo al que pertenece el alumno "+nombre_alumno);
        grupo=entrada.nextLine();
        grupo=grupo.toUpperCase();
        
        System.out.println("Ingresa cuantas calificaciones quieres capturar");
        int numero_calificaciones=entrada.nextInt();
        entrada.nextLine();

        for(int i=1; i <numero_; i++)

        
        
       }
        //calcular edad
        // int anioActual = java.time.Year.now().getValue();
        //length longitud si
        
